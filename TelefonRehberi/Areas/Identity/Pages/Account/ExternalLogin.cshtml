@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Kayıt Ol";
}
<link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/jodit/3.1.92/jodit.min.css">
<script src="//cdnjs.cloudflare.com/ajax/libs/jodit/3.1.92/jodit.min.js"></script>
<section class="ab-info-main py-md-5">
    <div class="container py-md-3">
        <h1>Kayıt Ol</h1>
        <h4>Hesap @Model.LoginProvider ekle.</h4>
        <hr />
        <p class="text-info">
            Hesabınızı şununla başarılı bir şekilde doğruladınız: <strong>@Model.LoginProvider</strong>.
            Lütfen bu site için aşağıya e-posta adresinizi girin ve başarılı bir şekilde giriş yapmak için Kayıt Ol butonuna tıklayın.
        </p>
        <div class="row">
            <div class="col-md-4">
                <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                    <div asp-validation-summary="All" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Input.Email">E-Posta</label>
                        <input asp-for="Input.Email" class="form-control" />
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn btn-primary">Kayıt Ol</button>
                </form>
            </div>
        </div>
    </div>
</section>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
    </script>
}